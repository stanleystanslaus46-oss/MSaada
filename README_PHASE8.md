# MSAADA — Phase 8

Phase 8 connects the Android PRO purchase flow to a secure Netlify verification backend.

The app does not contain a Google service-account secret. The backend verifies purchase tokens using the Google Play Developer API.

See `BILLING_BACKEND_SETUP.md` and `PHASE8_BILLING_BACKEND_AUDIT.md` before deployment.
